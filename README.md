# README

Ruby version: 2.4.1

yarn must be installed, for example through brew ( `brew install yarn` )

for local run:
```
$ gem install bundler
$ bundle
$ ./bin/rails db:migrate  
$ ./bin/server
```